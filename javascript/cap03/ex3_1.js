const prompt = require('prompt-sync')();
const number1 = Number(prompt('1º número: '));
const number2 = Number(prompt('2º númeo: '));
const soma = number1 + number2;
console.log(`A soma dos números: ${soma}`);

